node_modules/
dist/
build/
.next/
next-standalone.tar.gz
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
*.local
.DS_Store

# Playwright artefacts (may contain session tokens — never commit)
/playwright-report/
/test-results/
/playwright/.clerk/
/blob-report/

# Agent hook audit logs and local state
.cursor/hooks/logs/
.cursor/hooks/state/

# Guardrail upgrade — local artefacts (never commit)
.cursor/guardrail-baseline.log
